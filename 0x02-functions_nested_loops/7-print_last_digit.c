#include "main.h"
/**
 * print_last_digit - prints the last
 * digits of a number
 *
 * @w: input number as an integer
 *
 * Return: last digit
 */

int print_last_digit(int w)
{
	int ld;

	ld = w % 10;
	if (l > 0)
	{
		_putchar(-l + 48);
		return (-l);
	}
	else
	{
		_putchar(l + 48);
		return (l);
	}
	_putchar('\n');
}
